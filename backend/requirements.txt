# 📦 requirements.txt for BLISS

# Backend
fastapi==0.115.2
uvicorn==0.34.0
sqlglot==22.1.0
duckdb==0.10.2

# Frontend
streamlit==1.34.0
plotly==5.21.0
pandas==2.2.2
requests==2.31.0

# For Local Development Convenience
python-dotenv==1.0.1  # (optional, only if we later use .env files)

# Optional Nice-to-Haves (future)
# tqdm==4.66.2  # For progress bars if needed
# watchdog==4.0.0  # Faster dev server reloads for Streamlit
